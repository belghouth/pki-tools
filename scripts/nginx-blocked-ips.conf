# nginx geo block — auto-generated by cron/nginx_block_sync.php
# Deploy to: /etc/nginx/conf.d/blocked-ips.conf
# Must be in conf.d/ (http{} context) so $blocked_ip is available to all vhosts.
#
# One-time setup:
#   sudo cp scripts/nginx-blocked-ips.conf /etc/nginx/conf.d/blocked-ips.conf
#   sudo chown www-data:www-data /etc/nginx/conf.d/blocked-ips.conf

# Entries: 0
geo $blocked_ip {
    default 0;
}
